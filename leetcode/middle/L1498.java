class L1498 {
    // 感觉是滑动窗口
    // 看了一眼，排序后再搞就好多了，只需要知道有多少个子序列就好，与顺序无关
    public int numSubseq(int[] nums, int target) {
        int left = 0;
        int ans = 0;
        for(int right = 0; right < nums.length; right++) {

        }
    }
}